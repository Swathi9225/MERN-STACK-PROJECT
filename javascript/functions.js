/*const user = function( ){ 

    let Username= prompt("Enter the Username") ;
        if(Username!== "swathi"){
          console.log("Re-enter the Username");
          return;
        }
        
        else{
            console.log("Welcome to the site");
        } 
            
    let mail= prompt("enter the mail");
        if(mail!=="swathi123gmail.com"){
            console.log("try again!!");
            return;
         }
         else{
            console.log("mail id is verifide!!");
         }
            let chances = 0;
            while(chances < 3){
    let Password= prompt("Enter your password");
            if(Password==="Swathi12"){
                alert("Password is Saved");
                break;
            }
            else{
                chances++;
                console.log(`Recheck the Password (${chances}/3)`);
            }
            
        }
        if(chances=== 3)
                  {
                    console.log(" Too many attempts!!! So please wait for 30 second");
                  }
                 
         console.log("Thank You!!...");
                }

              user("swathi", "Swathi123gmail.com");*/
    
              


         
/*const find_min_max = function (...nums) {
    let min= nums[0];
    let max = nums[0];
    for(let i=1; i< nums.length; i++){
        if(nums[i]<min){
            min=nums[i];
        }
        if(nums[i]>max){
            max=nums[i];
        }
    }
        console.log(`The minimum value is : ${min}`);
        console.log(`The maximun value is : ${max}`);
         }

    find_min_max(1, 10, 3, 85, 25, 94, 9);

    
 
const find_max= (x, y, z) => {
 return Math.max(x, y, z);
}
console.log(`The maxmun value is : ${find_max(5, 2, 3)}`);


const find_min = (a,b,c)  =>{
    return Math.min(a,b,c);
} 
console.log(`The minmun value is : ${find_min(5, 2, 3)}`);

                
   const add = (...nums) =>  {
    let sum = 0;
    for(let i=0; i<nums.length; i++){
        sum += nums[i];
    }
    return sum;
   }     
   console.log(`The sum of numbers is:${add(10, 9,  3, 2, 1)}`);
*/
   
               